Candidate: Tajung Jang
email: tyj27@drexel.edu

! Assumes that 'actions.txt" is in project's root directory !

instructions to run:
    make && ./bin/simple_cross

instructions to remove object files and binaries:
    make clean
